#ECS-PAN mappings are the mappings that correspond to ANY log type/index name of PAN
#To install, use command:
bash create_ecs_pan_mapping
#If you want to create a separate mapping for each log type/index name (pan-traffic and pan-threat, etc.) use command:
bash create_panw.traffic_mapping
bash create_panw.traffic_mapping
#etc.
#etc.
